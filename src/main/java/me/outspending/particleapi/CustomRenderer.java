package me.outspending.particleapi;

/**
 * Represents a custom renderer.
 * <p>
 * This interface is used to create custom renderers that can be used to render custom
 * particle types. Custom renderers extends what you can do with this API. For example,
 * you can create a custom renderer that renders a particle type in a specific way.
 * With custom events, you can create a custom renderer that renders a particle type.
 *
 * @see CustomParticleType
 * @see CustomParticle
 * @see ParticleOptions
 * @see me.outspending.particleapi.renderer.Renderer
 * @since 1.0
 */
public interface CustomRenderer {
}
